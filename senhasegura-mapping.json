{
  "access_keys": [
    {
      "name": "AWS_VARIABLES",
      "type": "aws",
      "fields": {
        "access_key_id": "AWS_KEY",
        "secret_access_key": "AWS_SECRET_ACCESS_KEY_VARIABLE"
      }
    }
       {
      "name": "AZURE_VARIABLES",
      "type": "azure",
      "fields": {
        "access_key_id": "AZURE_ACCESS_KEY_ID_VARIABLE",
        "secret_access_key": "AZURE_SECRET_ACCESS_KEY_VARIABLE"
      }
    }
   {
      "name": "GCP_VARIABLES",
      "type": "gcp",
      "fields": {
        "access_key_id": "GCP_ACCESS_KEY_ID_VARIABLE",
        "secret_access_key": "GCP_SECRET_ACCESS_KEY_VARIABLE"
      }
    }
  ],
  "credentials": [
    {
      "name": "CREDENCIAL_VARIABLES",
      "fields": {
        "user": "USERNAME",
        "password": "PASSWORD",
        "host": "DATABASE_IP"
      }
    }
  ],
  "key_value": [
    {
      "name": "SECRET_VARIABLES",
      "fields": ["MAPPING_TEST"]
    }
  ]
}
